{% if collections %}
    <div class="collections-bar__wrapper">
        <ul>
            {% for collection in collections %}
                <li>
                    <a href="{{ collection.url }}">{{ collection.title }}</a>
                </li>
            {% endfor %}
        </ul>
    </div>
{% endif %}